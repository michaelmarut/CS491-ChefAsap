import { useState } from 'react';
import { ScrollView, Text, View } from "react-native";

export default function BookingsScreen() {
    return (
        <ScrollView className="flex-1 bg-base-100 p-5 pt-16">
            <Text> messages here hello </Text>
            <View className="h-24" />
        </ScrollView>
    );

}